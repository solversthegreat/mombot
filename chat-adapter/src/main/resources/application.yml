server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/
    registerWithEureka: true
    fetchRegistry: true
  instance:
    appname: rocket-chat

springdoc:
  swagger-ui:
    path: /docs